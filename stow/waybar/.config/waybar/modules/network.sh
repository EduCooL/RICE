#!/usr/bin/env bash
set -euo pipefail

# Native Waybar network is used in config.jsonc. This placeholder is kept for
# future custom display logic if native formatting becomes too limited.
printf '%s\n' ''

