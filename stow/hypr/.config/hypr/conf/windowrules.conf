# Conservative floating rules for utility windows and dialogs.
windowrule {
    match:class = ^(pavucontrol)$
    float = true
    size = 760 520
    center = true
}

windowrule {
    match:class = ^(blueman-manager)$
    float = true
    center = true
}

windowrule {
    match:class = ^(nm-connection-editor)$
    float = true
    center = true
}

windowrule {
    match:title = ^(Open File)(.*)$
    float = true
}

windowrule {
    match:title = ^(Save File)(.*)$
    float = true
}

windowrule {
    match:title = ^(Choose File)(.*)$
    float = true
}

windowrule {
    match:title = ^(File Upload)(.*)$
    float = true
}

windowrule {
    match:title = ^(Picture-in-Picture)$
    float = true
    pin = true
    keep_aspect_ratio = true
}

# Idle inhibit rules changed across Hyprland versions.
# Keep this handled by hypridle/app requests instead of adding fragile rules here.
